<UserControl x:Class="ToolBox.Controls.DraggableItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:theme="clr-namespace:YukkuriMovieMaker.Theme;assembly=YukkuriMovieMaker.Plugin"
             xmlns:commons="clr-namespace:ToolBox.Commons"
             xmlns:settings="clr-namespace:ToolBox.Settings"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="40"
             Cursor="Hand">

    <UserControl.Resources>
        <commons:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <!-- Iconモード：元のViewを表示 -->
        <Border Margin="2" x:Name="IconViewBorder">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DisplayMode}" Value="{x:Static settings:ToolItemDisplayMode.Custom}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <ContentPresenter Content="{Binding}" />
        </Border>

        <!-- Customモード：カスタム画像またはテキストを配置したボタンを表示 -->
        <Button x:Name="CustomButton"
                Command="{Binding DefaultCommand}"
                ToolTip="{Binding Label}"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                Margin="2">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DisplayMode}" Value="{x:Static settings:ToolItemDisplayMode.Custom}">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <!-- Custom Mode: Image -->
                <Image Source="{Binding CustomImagePath}"
                       Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Image.Style>
                        <Style TargetType="Image">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding CustomImagePath, Converter={StaticResource StringToVisibilityConverter}}" Value="Visible"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>

                <!-- Custom Mode: Text (画像がない場合) -->
                <TextBlock Text="{Binding Label}" TextTrimming="CharacterEllipsis" FontSize="11" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding CustomImagePath, Converter={StaticResource StringToVisibilityConverter}}" Value="Collapsed"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Button>

        <Border x:Name="ResizeHandle"
                Width="12" Height="12"
                CornerRadius="6"
                HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Margin="0,0,-6,-6"
                Cursor="SizeNWSE"
                Visibility="Collapsed"
                PreviewMouseLeftButtonDown="ResizeHandle_MouseLeftButtonDown">
            <Border.Background>
                <SolidColorBrush Color="{Binding Source={x:Static settings:ToolBoxSettings.Default}, Path=HighlightColor}" />
            </Border.Background>
        </Border>
    </Grid>
</UserControl>
