<UserControl x:Class="ToolBox.Items.TachiePlacer.Views.TachiePlacerSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
             xmlns:local="clr-namespace:ToolBox.Items.TachiePlacer.Settings"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:TachiePlacerSettings}"
             Width="240">

    <Border Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
            BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="8">

        <StackPanel>
            <TextBlock Text="立ち絵配置の設定" FontWeight="SemiBold" Margin="0,0,0,8" />

            <Grid Margin="0,0,0,6">
                <TextBlock Text="配置レイヤー" VerticalAlignment="Center" FontSize="11"/>
                <controls:EnumComboBox Value="{Binding Placement, Mode=TwoWay}" EnumType="{x:Type local:TachiePlacement}" Width="120" HorizontalAlignment="Right"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
