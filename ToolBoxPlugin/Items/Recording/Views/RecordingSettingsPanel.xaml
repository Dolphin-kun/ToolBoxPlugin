<UserControl x:Class="ToolBox.Items.Recording.Views.RecordingSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
             xmlns:recSetting="clr-namespace:ToolBox.Items.Recording.Settings"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=recSetting:RecordingSettings}"
             Width="280">


    <Border Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
            BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="8">

        <StackPanel>
            <TextBlock Text="録音の設定" FontWeight="SemiBold" Margin="0,0,0,6" />

            <TextBlock Text="入力デバイス" FontSize="11" Margin="0,0,0,2" />
            <controls:CommonComboBox x:Name="DeviceComboBox"
                                     ItemsSource="{Binding DeviceList, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                     SelectedValuePath="Key"
                                     DisplayMemberPath="Value"
                                     Value="{Binding RecordingDeviceIndex, Mode=TwoWay}"
                                     HorizontalAlignment="Stretch" />
        </StackPanel>
    </Border>
</UserControl>
